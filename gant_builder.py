# use matplot.savefig after rendering put the figures in the /figures folder, with prefix date_time_filename


#def gant_builder(reconstruction, num_jobs, num_machines):


# See function: plot_JSSP_Gantt(reconstruction, JSSP_cur.jobs_num, JSSP_cur.machines_num)